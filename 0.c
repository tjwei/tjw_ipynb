#include <stdio.h> /* 
echo "Hello world!" << } && exit

It doesn't matter you use vim, emacs, or echo.
Real programmers use one-way functions to compile programs.
 
$ sh 0.c
$ gcc 0.c&&./a.out
$ sha384sum 0.c| xxd -r -p >a.out &&chmod a+x a.out 
$ ./a.out 
 
sha384 was chosen because we can fit an ELF in the digest!(48>45) 
*/
main() {
 puts("Hello world!");
}
 